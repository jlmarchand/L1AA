\documentclass{standalone}
\usepackage{tikz}
\usetikzlibrary{matrix}

\begin{document}

\tikzset{every node/.style={text=white},
  every left delimiter/.style={color=white},
  every right delimiter/.style={color=white},
    node style sp/.style={draw,circle, text = white},
    node style ge/.style={circle, text = white},
    arrow style mul/.style={draw,sloped,midway,fill=white},
    arrow style plus/.style={midway,sloped,fill=white}}

\begin{tikzpicture}[baseline=(A.center)]

% les matrices

\matrix (A) [matrix of math nodes, nodes = {node style ge},
             left delimiter  = (,
             right delimiter = ),
                column sep = 0pt ] 
{2 & 1 & -1 &3  \\
-1 & 0 &2 & 5 \\ 
};

\matrix (B) [matrix of math nodes, nodes = {node style ge},%
             left delimiter  = (,%
             right delimiter = ),column sep=0 mm] at (5,4)
{ 1 & 2 & 3  \\
-1 & -2 &5 \\
2 & -3 &1 \\
-1 & 0 &0 \\
};

\matrix (C) [matrix of math nodes, nodes = {node style ge},%
             left delimiter  = (,%
             right delimiter = ),column sep=0 mm] at (5,0)
{ ? &? &? \\
? &? &? \\
};
\end{tikzpicture}
\end{document}
